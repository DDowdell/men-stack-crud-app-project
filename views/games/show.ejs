<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />

    <title><%= game.title %></title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= game.title %></h1>
    <p>Category: <%= game.category %></p>
    <p>Hours Played: <%= game.hoursPlayed %></p>
    <p>Level Reached: <%= game.levelReached %></p>
    <% if (game.notes) { %>
    <p>Notes: <%= game.notes %></p>
    <% } %> 
    
      <% if (user._id.toString() === currentUser._id.toString()) { %>
    <a href="/users/<%= user._id %>/games/<%= game._id %>/edit">Edit Game Details</a>

    
    <form
    action="/users/<%= user._id %>/games/<%= game._id %>?_method=DELETE"
    method="POST"
    >
    <button type="submit">Delete Game</button>
  </form>
  <% } %>

    
</body>
</html>